import { Box } from '@/components/box';
import { Pressable } from 'react-native';

export function ActivityCard(isFinished: boolean) {
  return (
    <Pressable>
      <Box></Box>
    </Pressable>
  );
}
